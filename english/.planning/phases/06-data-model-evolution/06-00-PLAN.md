---
phase: 06-data-model-evolution
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/word-senses.test.ts
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Test stub file exists and is runnable (passes with skipped/todo tests)"
  artifacts:
    - path: "tests/word-senses.test.ts"
      provides: "Integration test stubs for word senses"
      contains: "describe"
  key_links: []
---

<objective>
Create test stub file for word senses integration tests required by the Nyquist validation contract.

Purpose: Wave 0 prerequisite -- tests/word-senses.test.ts must exist before execution plans can reference it in their verify steps.
Output: Test stub file with describe blocks and todo tests for DATA-01 (multi-POS insertion) and DATA-02 (independent SRS per sense).
</objective>

<execution_context>
@/Users/evd3v/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evd3v/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-data-model-evolution/06-RESEARCH.md

<interfaces>
<!-- Test helper pattern used across existing tests -->

From tests/helpers/setup.ts:
```typescript
export async function buildTestApp(): Promise<FastifyInstance>;
export async function cleanupTestApp(app: FastifyInstance): Promise<void>;
```

From tests/db.test.ts (pattern reference):
```typescript
import { createDb, type Database } from '../src/db/index.ts';
import * as schema from '../src/db/schema/index.ts';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create word-senses test stub file</name>
  <files>tests/word-senses.test.ts</files>
  <action>
Create `tests/word-senses.test.ts` with integration test stubs following the existing test patterns (see tests/db.test.ts and tests/words-api.test.ts for conventions).

Use `buildTestApp` / `cleanupTestApp` from `./helpers/setup.ts`.

Structure:

```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import type { FastifyInstance } from 'fastify';
import { buildTestApp, cleanupTestApp } from './helpers/setup.ts';

let app: FastifyInstance;

beforeAll(async () => {
  app = await buildTestApp();
});

afterAll(async () => {
  await cleanupTestApp(app);
});

describe('Word senses - DATA-01: Multi-POS insertion', () => {
  it.todo('inserts the same lemma with two different POS values without constraint violation');
  it.todo('each sense has independent translation');
  it.todo('composite unique (word_id, part_of_speech) prevents duplicate senses');
});

describe('Word senses - DATA-02: Independent SRS per sense', () => {
  it.todo('creates separate SRS cards for different senses of the same word');
  it.todo('reviewing one sense card does not affect another sense card for the same word');
});
```

All tests are `it.todo(...)` stubs -- they will be implemented during plan 06-01 and 06-02 execution as the schema and routes become available.
  </action>
  <verify>
    <automated>npm test -- tests/word-senses.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>tests/word-senses.test.ts exists, vitest recognizes it and reports todo tests (no failures).</done>
</task>

</tasks>

<verification>
- `tests/word-senses.test.ts` exists on disk
- `npm test -- tests/word-senses.test.ts` runs without error (todos are not failures)
</verification>

<success_criteria>
- Test file exists with correct imports and describe/it.todo structure
- vitest can parse and run the file without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-model-evolution/06-00-SUMMARY.md`
</output>
