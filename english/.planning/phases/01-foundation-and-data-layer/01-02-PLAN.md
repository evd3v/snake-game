---
phase: 01-foundation-and-data-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/server.ts
  - src/main.ts
  - src/lib/redis.ts
  - src/plugins/db.ts
  - src/plugins/queue.ts
  - src/routes/health.ts
  - src/workers/index.ts
  - vitest.config.ts
  - tests/helpers/setup.ts
  - tests/health.test.ts
  - tests/db.test.ts
  - tests/queue.test.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-06

must_haves:
  truths:
    - "Fastify server starts and responds 200 to GET /health"
    - "BullMQ worker connects to Redis and processes a test job to completion"
    - "Database connection is established and schema tables are queryable"
    - "All integration tests pass"
  artifacts:
    - path: "src/server.ts"
      provides: "Fastify server factory"
      exports: ["buildServer"]
      min_lines: 15
    - path: "src/main.ts"
      provides: "Application entry point"
      min_lines: 5
    - path: "src/workers/index.ts"
      provides: "BullMQ worker process"
      contains: "Worker"
    - path: "src/routes/health.ts"
      provides: "Health check endpoint"
      contains: "/health"
    - path: "tests/health.test.ts"
      provides: "Health endpoint test"
      contains: "test"
    - path: "tests/queue.test.ts"
      provides: "Queue integration test"
      contains: "test"
  key_links:
    - from: "src/server.ts"
      to: "src/plugins/db.ts"
      via: "@fastify/autoload loading plugins directory"
      pattern: "autoload.*plugins"
    - from: "src/plugins/db.ts"
      to: "src/db/index.ts"
      via: "imports createDb and decorates Fastify"
      pattern: "createDb|decorate.*db"
    - from: "src/plugins/queue.ts"
      to: "src/lib/redis.ts"
      via: "imports shared Redis connection"
      pattern: "redisConnection"
    - from: "src/workers/index.ts"
      to: "src/lib/redis.ts"
      via: "imports shared Redis connection config"
      pattern: "redisConnection"
    - from: "tests/health.test.ts"
      to: "src/server.ts"
      via: "imports buildServer for inject testing"
      pattern: "buildServer"
---

<objective>
Build the Fastify API server with database and queue plugins, BullMQ worker process, and integration tests covering all infrastructure requirements.

Purpose: Complete the running development environment by adding the application server layer (Fastify + plugins), the async job processing layer (BullMQ worker), and validate everything works via integration tests.

Output: A Fastify server responding to health checks, a BullMQ worker ready for job processing, and a green test suite proving INFRA-02 and INFRA-06.
</objective>

<execution_context>
@/Users/evd3v/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evd3v/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-and-data-layer/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From src/db/index.ts:
```typescript
export function createDb(databaseUrl: string): ReturnType<typeof drizzle>;
export type Database = ReturnType<typeof createDb>;
```

From src/db/schema/index.ts:
```typescript
// Barrel export of all tables: sentences, words, sentenceWords, collocations,
// sentenceCollocations, grammarPatterns, sentenceGrammarPatterns, wordFamilies,
// srsCards, and all enums (familiarityEnum, collocationTypeEnum, cardTypeEnum, cardStateEnum)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fastify server, plugins, routes, and BullMQ worker</name>
  <files>src/server.ts, src/main.ts, src/lib/redis.ts, src/plugins/db.ts, src/plugins/queue.ts, src/routes/health.ts, src/workers/index.ts</files>
  <action>
1. Create src/lib/redis.ts:
   - Import IORedis from ioredis
   - Export a function `createRedisConnection(url?: string)` that returns new IORedis instance with maxRetriesPerRequest: null (required for BullMQ)
   - Default url from process.env.REDIS_URL
   - Export a lazy singleton `redisConnection` for convenience

2. Create src/plugins/db.ts:
   - Import fp from fastify-plugin (MUST use fp wrapper so decorator is visible to routes)
   - Import createDb from ../db/index.js
   - Create db instance via createDb(process.env.DATABASE_URL!)
   - Decorate Fastify with 'db'
   - Add onClose hook to end the db connection pool if applicable
   - Declare module augmentation for FastifyInstance to add db typing

3. Create src/plugins/queue.ts:
   - Import fp from fastify-plugin
   - Import { Queue } from bullmq
   - Import redis connection from ../lib/redis.js
   - Create Queue named 'sentence-analysis' with the redis connection
   - Decorate Fastify with 'analysisQueue'
   - Add onClose hook to close the queue
   - Declare module augmentation for FastifyInstance to add analysisQueue typing

4. Create src/routes/health.ts:
   - Export default async FastifyPluginAsync
   - Register GET /health that returns { status: 'ok', timestamp: new Date().toISOString() }
   - Do NOT wrap with fp (routes should be encapsulated)

5. Create src/server.ts:
   - Export async function buildServer(opts?: { skipPlugins?: boolean })
   - Create Fastify instance with logger: true (or opts-based)
   - Register @fastify/sensible
   - Register @fastify/cors
   - Register @fastify/autoload for plugins dir (path.join(import.meta.dirname, 'plugins'))
   - Register @fastify/autoload for routes dir (path.join(import.meta.dirname, 'routes'))
   - Return app

6. Create src/main.ts:
   - Import dotenv/config
   - Import buildServer
   - Build server, listen on port 3000, host 0.0.0.0
   - Handle startup errors with process.exit(1)

7. Create src/workers/index.ts:
   - Import dotenv/config
   - Import { Worker } from bullmq
   - Import redis connection from ../lib/redis.js (create a NEW connection, do not share with queue)
   - Create Worker for 'sentence-analysis' queue
   - Processor: log job data, return { status: 'completed' } (placeholder for Phase 2)
   - Add completed and failed event handlers with console.log/error
   - Add graceful shutdown: process.on('SIGTERM', () => worker.close())
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && echo "TypeScript OK"</automated>
  </verify>
  <done>Fastify server factory exported from server.ts, main.ts entry point configured, DB and queue plugins decorated on Fastify instance with fp wrapper, health route returns 200, worker process ready, all files compile without type errors</done>
</task>

<task type="auto">
  <name>Task 2: Vitest configuration and integration tests</name>
  <files>vitest.config.ts, tests/helpers/setup.ts, tests/health.test.ts, tests/db.test.ts, tests/queue.test.ts</files>
  <action>
1. Create vitest.config.ts:
   - Import defineConfig from vitest/config
   - Set test.globals: false (explicit imports)
   - Set test.environment: 'node'
   - Set test.testTimeout: 10000 (integration tests need time)
   - Set test.setupFiles: [] (no global setup needed)

2. Create tests/helpers/setup.ts:
   - Export async function buildTestApp() that imports buildServer from src/server.ts and returns a ready Fastify instance (call await app.ready())
   - Export async function cleanupTestApp(app) that calls await app.close()
   - These helpers avoid repeating setup/teardown in every test file

3. Create tests/health.test.ts (covers INFRA-02):
   - Import { describe, it, expect, beforeAll, afterAll } from vitest
   - Import buildTestApp, cleanupTestApp from helpers
   - beforeAll: build app
   - afterAll: cleanup app
   - Test: GET /health returns 200 with { status: 'ok', timestamp: string }
   - Use app.inject({ method: 'GET', url: '/health' }) -- Fastify inject, no HTTP server needed
   - Note: The db and queue plugins will attempt real connections. For this test to work without Docker, either:
     a) Set DATABASE_URL and REDIS_URL env vars pointing to running services, OR
     b) Skip the db/queue plugins in test mode. Simplest approach: make buildServer accept an options object { skipPlugins?: string[] } and conditionally skip autoload. BUT this is complex -- instead, just document that tests require `docker compose up postgres redis` first.
   - Add a comment at top of test file: "Requires: docker compose up -d postgres redis"

4. Create tests/db.test.ts (covers INFRA-01):
   - Import createDb from src/db/index.ts
   - Import { sql } from drizzle-orm
   - beforeAll: create db connection to DATABASE_URL
   - Test "schema tables exist": run db.execute(sql`SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'`) and verify expected tables (sentences, words, collocations, grammar_patterns, word_families, srs_cards, sentence_words, sentence_collocations, sentence_grammar_patterns) are present
   - Test "can insert and query a sentence": insert a test row into sentences, query it back, verify fields
   - afterAll: clean up test data, close connection
   - Note: requires migrations to have been run first. Add comment: "Requires: npm run db:push (or db:migrate) against running PostgreSQL"

5. Create tests/queue.test.ts (covers INFRA-06):
   - Import { Queue, Worker } from bullmq
   - Import redis connection factory from src/lib/redis.ts
   - Test "can enqueue and process a job":
     a) Create a Queue and Worker for a test queue name (e.g., 'test-queue')
     b) Add a job with test data
     c) Wait for worker 'completed' event (use a Promise with setTimeout fallback for 5s timeout)
     d) Verify the job was processed (completed event fires)
     e) Close queue and worker in afterAll
   - Use separate Redis connections for Queue and Worker (BullMQ requirement)
   - Add comment: "Requires: Redis running on REDIS_URL"

6. All tests should load dotenv/config or set env vars. Add a test script env loading approach: in vitest.config.ts add `test.env` or use `dotenv` in setupFiles if needed. Simplest: `import 'dotenv/config'` at top of each test file, with .env file containing local connection strings.
  </action>
  <verify>
    <automated>docker compose up -d postgres redis && sleep 3 && DATABASE_URL=postgres://english:english@localhost:5432/english npm run db:push && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 test files pass: health endpoint returns 200, database schema tables exist and are queryable, BullMQ job is enqueued and processed. Vitest config is in place. Wave 0 validation gaps from 01-VALIDATION.md are all covered.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all tests green
2. `curl http://localhost:3000/health` returns `{"status":"ok","timestamp":"..."}` (with app running)
3. Worker process starts without errors: `npx tsx src/workers/index.ts` (connects to Redis, waits for jobs)
4. `npx tsc --noEmit` -- no type errors
</verification>

<success_criteria>
- Fastify server starts and responds to GET /health with 200
- BullMQ worker connects to Redis and processes a test job
- Database tables are created and queryable via Drizzle
- All integration tests pass (health, db, queue)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-layer/01-02-SUMMARY.md`
</output>
